Włączenie/wyłączenie farmy wiatrowej albo PV.

```jsx
<TogglePill on={windOn} onChange={setWindOn} />
```

Obok pastylki podaj produkcję z prognozy i słowo `AUTO` (`~320 AUTO`) — moc OZE wynika z pogody, nie z decyzji gracza.
